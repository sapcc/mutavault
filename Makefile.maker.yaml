metadata:
  url: https://github.com/sapcc/mutavault

binaries:
  - name:        mutavault
    fromPackage: .
    installTo:   bin/

golangciLint:
  createConfig: true

githubWorkflow:
  ci:
    enabled: true
  global:
    defaultBranch: master

renovate:
  enabled: true
