metadata:
  url: https://github.com/sapcc/mutavault

binaries:
  - name:        mutavault
    fromPackage: .
    installTo:   bin/

golangciLint:
  createConfig: true

githubWorkflow:
  global:
    defaultBranch: master
  license:
    enabled: true
  securityChecks:
    enabled: true
